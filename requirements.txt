# Core Dependencies
torch                 # PyTorch (Required for GPT-2)
transformers          # Hugging Face Transformers for GPT-2
datasets              # Hugging Face Datasets (for text loading)
tokenizers           # Tokenizer support for GPT models

# Flask API
flask                 # Flask for API
flask-cors            # Enable CORS for API access

# Gradio for Chat UI
gradio               # Interactive chatbot interface

# Additional Utilities
numpy                # Matrix operations
pandas               # Data handling
tqdm                 # Progress bar for training
